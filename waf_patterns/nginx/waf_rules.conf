# Generated by fabriziosalmi/patterns from OWASP CoreRuleSet (v4.29.0), Apache-2.0; converted to Nginx.
# Source rules: https://github.com/coreruleset/coreruleset
# Derived/converted work redistributed under Apache-2.0. See THIRD_PARTY_NOTICES.md.
#
# Nginx WAF Rules (Generated by json2nginx.py)
#
# Include this file inside a `server` or `location` block.
# Requires waf_maps.conf to be included in the `http` block.
#
# Requests matching a `high` severity pattern are refused with 403.
# Lower severities are recorded in the variables but do not block:
# they hold "<severity>:<category>" and are usable in log_format.

if ($waf_args ~ "^high") {
  return 403;
}
if ($waf_http_content_type ~ "^high") {
  return 403;
}
if ($waf_http_host ~ "^high") {
  return 403;
}
if ($waf_http_referer ~ "^high") {
  return 403;
}
if ($waf_http_user_agent ~ "^high") {
  return 403;
}
if ($waf_request_uri ~ "^high") {
  return 403;
}
